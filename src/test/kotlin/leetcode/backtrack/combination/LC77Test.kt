package leetcode.backtrack.combination

import org.junit.Test

import org.junit.Assert.*

class LC77Test {

    @Test
    fun combine() {
        val combine = LC77().combine(4, 2)
        println(combine)
//        LC77().combine(1,1)
    }
}